{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for OCR Lab",
  "windows": ["main", "virtual-screen", "tool-overlay", "relic-overlay", "relic-pick-overlay"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-set-size",
    "dialog:default",
    "dialog:allow-open"
  ]
}
